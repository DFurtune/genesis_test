/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  basePath: '/genesis_test',
  assetPrefix: '/genesis_test/',
};

module.exports = nextConfig;